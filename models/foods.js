//////////////////////////////////
// Foods Model
//////////////////////////////////


const mongoose = require('./connection');

const { Schema, model } = mongoose

const foodsSchema = new Schema({

})